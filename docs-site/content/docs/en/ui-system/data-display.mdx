---
title: Data Display
description: Tables, ranked charts, cards, separators, typography, and ECharts theme adapter.
icon: Table2
---

Data display components power provider lists, model catalogs, usage dashboards, and chart surfaces.

## Chart adapter

The ECharts package provides shared usage trend chart models and theme palettes:

```text
CodexSwitchUI/src/CodexSwitchUI.ECharts
```

Use it for usage trend views rather than duplicating chart formatting inside pages.
